// utilizando o objeto math
let num1 = 9.45342;

// arrendondando um número

// arredondando para baixo
//let num2 = Math.floor(num1);

// arredondando para cima
//let num3 = Math.ceil(num1);

// arredondando para o mais proximo
//let num4 = Math.round(num1);

// função do objeto math que retorna o maior e o menor número de uma sequência de números.
//console.log(Math.max(1, 3, 5, 9, 20, 11)); retorna o maior
//console.log(Math.min(1, 3, 5, 9, 3, 2, 10, 12)); retorna o menor

// Gerando um número aleátorio com o método random do objeto matemática
//console.log(Math.random()); nesse exemplo ele ira gerar um número entre 0 e 1 sendo que o ultimo não é incluido. se quisermos gerar dentro de um intervalo utilizamos a formula
//console.log(Math.random() * (10 - 5) + 5);  nesse caso estamos gerando um número entre 10 e 5. a formula geral é colocar o método que vai multiplicar o intervalo definido (subtração do maior e o menor) mais a soma do menor. 

// juntando conhecimentos dessa aula podemos fazer

// gerando um número aleatório entre 1 e 100
//console.log(Math.round(Math.random() * (100 - 1) + 1));

// constante PI. Observação: propriedade da objeto math
//console.log(Math.PI);

// método de potenciação que recebe dois parâmetros (Primeiro número será o valor que elevado e o segundo a potência). utilizando o método math
//console.log(Math.pow(2, 10));

// fazendo manualmente em contas
//console.log(2 ** 10);

// raiz quadrada utilizando as contas de matematicas
let num = 9;
console.log(num ** (1/2));


