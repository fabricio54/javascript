// Tipos de Dados Primitivos: String, number, undefined, null, boolean, symbol
const nome = 'Luiz';  // string
const nome1 = "Luiz"; // string
const nome2 = `Luiz`; // string
const num1 = 10;      // number 
const num2 = 10.52;   // number
let nomeAluno; // criamos mais não declaramos a variável por padrão ele inicializa com (undefined) = não aponta pra local nenhum na memória
let sobrenomeAluno = null; // Nulo -> Não aponta pra local nenhum na memória
const boolean = true; // receber true or false (variável booleana/lógico) 

// checando os tipos das variáveis
console.log(typeof nome);
console.log(typeof nome1);
console.log(typeof nome2);
console.log(typeof num1);
console.log(typeof num2);
console.log(typeof nomealuno);
console.log(typeof sobrenomeAluno);
console.log(typeof boolean);